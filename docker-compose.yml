version: '3.8'

services:
  zharnikov_cv:
    build: .
    container_name: zharnikov_cv
    restart: unless-stopped
    ports:
      - "127.0.0.1:3005:3000"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000"]
      interval: 30s
      timeout: 10s
      retries: 3
